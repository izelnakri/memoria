REGISTRY="ghcr.io" # set -a && source .env docker push ghcr.io/phanatic/app:1.0.0
REPO_OWNER="izelnakri"
PGUSER=postgres
PGPASSWORD=postgres
PGHOST=localhost
# CIRCLE_BRANCH=$$(if [ -v CIRCLE_BRANCH ]; then echo master; else git branch --no-color 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/\1/'; fi)
DOCKER_TAG=memoria:main
# DOCKER_TAG=$(echo memoria:${CIRCLE_BRANCH} | tr '/' '_')
# DOCKER_TAG=$DOCKER_TAG
